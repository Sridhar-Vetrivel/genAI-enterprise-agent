# E-13 — QA summary — routing accuracy and hallucination rate

| | |
|---|---|
| **Evidence ID** | **E-13** (cite this from Section 3) |
| **Deliverable** | D-07 (12 test queries, grounded and cited) |
| **Command** | `make qa` |

## Why this page exists

The 12 test-query pages prove the agents route correctly and answer with citations. They cannot prove this. Screenshot only the query pages and this deliverable goes into the submission unevidenced, which is precisely the inconsistency the evaluator cross-checks for.

## 📸 How to take the screenshot

1. Open a terminal wide enough that no line wraps.

2. Run the command, keeping it visible above its output:

   ```bash
   make qa
   ```

3. Check that these are all in the same frame:

   - the `Routing accuracy` line — the headline number for Section 6,
   - the `Hallucination rate` line, and the claim counts behind it,
   - the `Answers judged` line — a rate computed from unjudged answers is worthless, so this must read 12/12,
   - the per-query PASS lines above the summary,

4. Screenshot the **whole terminal window**, not a crop.

5. Paste it into the mid-term doc under its Evidence block, and fill the header table:

   | Field | Value |
   |---|---|
   | Evidence ID | E-13 |
   | What this proves | The graded QA run: routing accuracy and hallucination rate across all 12 queries |
   | Deliverable ID | D-07 |
   | Date captured | *(fill in)* |
   | Verifiable link | *(GitHub commit)* |

> A full run is ~60 local LLM calls and takes the best part of an hour on CPU. It checkpoints after every query, so if you only need the summary and the report is already built, `make evidence` reprints it without re-running anything.

---

*Generated by `make evidence`. Models: `gemma3:4b` / `gemma3:1b`, local Ollama, ₹0.*
